import { createVegetation } from './vegetation';
import { createObstacles } from './obstacles';

/** Jungle props are split into vegetation + obstacles; this file keeps the old import path. */
export { createVegetation, createObstacles };
